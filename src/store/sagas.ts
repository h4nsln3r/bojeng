import { all } from 'redux-saga/effects';
// import configurationSagas from './configuration/sagas';
// import generalSagas from './general/sagas';

export default function* sagas() {
    yield all([]);
    // yield all([configurationSagas(), generalSagas()]);
}
